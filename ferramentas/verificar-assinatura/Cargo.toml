[package]
name = "verificar-assinatura"
version = "0.1.0"
edition.workspace = true
publish = false

# Ferramenta de PORTAO, nao faz parte de nada que se instale. Ver src/main.rs.

[dependencies]
# A MESMA biblioteca que o tauri-plugin-updater usa para verificar. Escrita a mao, a
# ferramenta verificava um subconjunto do que o updater verifica -- ver src/main.rs.
minisign-verify = "0.2"
base64 = "0.22"
serde_json = "1"
anyhow = "1"
